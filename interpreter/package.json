{
    "name": "interpreter",
    "scripts": {
        "start": "swc src -d dist -w",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "commander": "^8.3.0",
        "get-stdin": "^9.0.0",
        "tree-sitter": "^0.20.0",
        "tree-sitter-lox": "file:../tree-sitter-lox"
    }
}
