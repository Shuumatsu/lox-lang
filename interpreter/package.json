{
    "name": "interpreter",
    "scripts": {
        "start": "swc src -d dist -w",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "tree-sitter": "^0.20.0",
        "tree-sitter-lox": "file:../tree-sitter-lox"
    }
}